// Generate LSC Mobile Metadata Cache — run on-demand
// Requires: Demo_MetadataGeneratorSchedulable + Demo_MetadataGeneratorCallout deployed to the org

new Demo_MetadataGeneratorSchedulable().execute(null);
